name: OrangeFox - Build

# Credits to:
# https://github.com/TeamWin
# https://gitlab.com/OrangeFox
# https://github.com/azwhikaru for Recovery Builder Template
# And all Contributors in every repositories I used

on:
  workflow_dispatch:
    inputs:
      MANIFEST_BRANCH:
        description: 'OrangeFox Manifest Branch'
        required: true
        default: '12.1'
        type: choice
        options:
        - 12.1
        - 11.0
      DEVICE_TREE:
        description: 'OrangeFox Device Tree'
        required: true
        default: 'https://github.com/neepntt9/android_device_samsung_m14x'
      DEVICE_TREE_BRANCH:
        description: 'OrangeFox Device Tree Branch'
        required: true
        default: 'android-12.1'
      DEVICE_PATH:
        description: 'Specify your Device Path'
        required: true
        default: 'device/samsung/m14x'
      DEVICE_NAME:
        description: 'Specify your Device Codename'
        required: true
        default: 'm14x'
      BUILD_TARGET:
        description: 'Specify your Build Target'
        required: true
        default: 'recovery'
        type: choice
        options:
        - boot
        - recovery
        - vendorboot

jobs:
  build:
    name: Build OFR by ${{ github.actor }}
    runs-on: ubuntu-22.04
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    permissions:
      contents: write
    steps:
    - name: Checkout
      uses: actions/checkout@v4
              
    - name: Clean-up
      uses: rokibhasansagar/slimhub_actions@main

    - name: Swap Space
      uses: pierotofy/set-swap-space@master
      with:
        swap-size-gb: 54
      
    - name: Build Environment
      run: |
        sudo apt install aria2 -y
        git clone https://gitlab.com/OrangeFox/misc/scripts.git -b master
        cd scripts
        sudo bash setup/android_build_env.sh
      
    - name: Set-up Manifest
      if: inputs.MANIFEST_BRANCH == '11.0' || inputs.MANIFEST_BRANCH == '12.1'
      run: |
        mkdir -p ${GITHUB_WORKSPACE}/OrangeFox
        cd ${GITHUB_WORKSPACE}/OrangeFox
        git config --global user.name "${{ github.actor }}"
        git config --global user.email "${{ github.actor_id }}+${{ github.actor }}@users.noreply.github.com"
        git clone https://gitlab.com/OrangeFox/sync.git -b master
        cd sync
        ./orangefox_sync.sh --branch ${{ inputs.MANIFEST_BRANCH }} --path ${GITHUB_WORKSPACE}/OrangeFox/fox_${{ inputs.MANIFEST_BRANCH }}

    - name: Clone Device Tree
      run: |
        cd ${GITHUB_WORKSPACE}/OrangeFox/fox_${{ inputs.MANIFEST_BRANCH }}
        git clone ${{ inputs.DEVICE_TREE }} -b ${{ inputs.DEVICE_TREE_BRANCH }} ./${{ inputs.DEVICE_PATH }}
        cd ${{ inputs.DEVICE_PATH }}
        echo "COMMIT_ID=$(git rev-parse HEAD)" >> $GITHUB_ENV

    - name: Building OrangeFox
      run: |
       cd ${GITHUB_WORKSPACE}/OrangeFox/fox_${{ inputs.MANIFEST_BRANCH }}
       set +e
export FOX_INTERNAL_RELEASE=R12.1 \
FOX_MAINTAINER=neeruzzfr@gmail.com \
FOX_DEVICE=m14x \
FOX_BUILD_VARIANT=Official \
FOX_MAINTAINER_PATCH_VERSION=12 \
FOX_ANDROID_VERSION=13 \
FOX_RECOVERY_STANDARD=13th \
FOX_BUILD_DEBUG_MESSAGES=1 \
FOX_NO_SAMSUNG_SPECIAL=0 \
FOX_DYNAMIC_SAMSUNG_FIX=1 \
OF_SAMSUNG_ODIN_TAR=1 \
OF_SEANDROIDENFORCE=1 \
FOX_SAMSUNG_RMM_BYPASS=1 \
OF_SAMSUNG_KNOX_DISABLE=1 \
FOX_SAMSUNG_DYNAMIC_PARTS=1 \
FOX_SAMSUNG_MTP_FIX=1 \
OF_SAMSUNG_ENCRYPTION=1 \
OF_ORANGEFOX_GSI_TOOLKIT=1 \
FOX_GSI_SYSTEM_RESIZER=1 \
OF_GSI_A_ONLY_MODE=1 \
FOX_GSI_VENDOR_DETECT=1 \
OF_GSI_DYNAMIC_RESIZE=1 \
FOX_GSI_IMAGE_CONVERTER=1 \
OF_GSI_SEPOLICY_PATCH=1 \
FOX_GSI_DUAL_BOOT=1 \
OF_GSI_LP_30_MODE=1 \
FOX_GSI_TREBLE_FIXER=1 \
OF_GSI_SHRINKER=1 \
FOX_GSI_MULTISLOT=0 \
OF_GSI_VBMETA_FIX=1 \
OF_USE_CRB_TOOL=1 \
FOX_GSI_SUPPORT=1 \
OF_PATCH_GSI_IMAGES=1 \
FOX_GSI_PARTITION="/dev/block/by-name/system" \
OF_SUPPORT_GSI_DYNAMIC_PARTS=1 \
FOX_FULL_GSI_MODE=1 \
OF_GSI_SYSTEM_RESIZER=1 \
FOX_GSI_AVB_BYPASS=1 \
OF_GSI_VENDOR_PATCH=1 \
FOX_USE_FULL_WIFI=1 \
OF_ENABLE_NETWORKING=1 \
FOX_BROWSER_ENABLE=1 \
OF_INTERNET_BROWSER=1 \
FOX_DOWNLOAD_MANAGER=1 \
OF_SUPPORT_DOWNLOADS=1 \
FOX_ARIA2_DOWNLOADER=1 \
OF_WIFI_SCAN=1 \
FOX_NETWORK_UTILS=1 \
FOX_ADVANCED_WIFI_SCAN=1 \
OF_WIFI_SCAN_INTERVAL=5 \
FOX_WIFI_SIGNAL_STRENGTH=1 \
OF_WIFI_AUTO_CONNECT=1 \
FOX_WIFI_SORT_BY_SIGNAL=1 \
OF_WIFI_HIDDEN_SSID=1 \
FOX_WIFI_CHANNEL_SCAN=1 \
OF_WIFI_5G_SUPPORT=1 \
FOX_WIFI_FREQUENCY_SCAN=1 \
OF_ANIMATION_ENABLE=1 \
FOX_ENABLE_ANIMATIONS=1 \
OF_BOOT_ANIMATION=1 \
OF_PROGRESS_BAR_ANIMATION=1 \
FOX_SPLASH_SCREEN_ANIMATION=1 \
OF_FLING_ANIMATIONS=1 \
OF_USE_NATIVE_FILE_MANAGER=1 \
OF_NATIVE_MTP=1 \
OF_NATIVE_ADB=1 \
FOX_NATIVE_PARTITION_MANAGER=1 \
OF_NATIVE_LOGGING=1 \
FOX_NATIVE_INSTALLER=1 \
OF_NATIVE_DECRYPT=1 \
FOX_NATIVE_THEMES=1 \
OF_PATCH_KERNELSU=1 \
FOX_KSU_SUPPORT=1 \
OF_KSU_INSTALLER=1 \
FOX_KSU_MODULES=1 \
OF_ENABLE_LPTOOLS=1 \
FOX_LPTOOLS_PARTITIONER=1 \
OF_LPTOOLS_REPACKER=1 \
FOX_LPTools_GSI_BUILDER=1 \
OF_DISABLE_MINIUI=1 \
FOX_NO_MINI_FEATURES=1 \
OF_FULLSCREEN_ONLY=1 \
FOX_DISABLE_MINI_PARTS=1 \
OF_NO_MINI_TOOLS=1 \
FOX_USE_TWRP_FLASH=1 \
OF_ALTERNATIVE_INSTALLER=1 \
FOX_MULTITOOL_SUPPORT=1 \
OF_USE_SEDUTILS=1 \
FOX_USE_BROTLI=1 \
OF_USE_LZMA=1 \
FOX_USE_RESET_PROP=1 \
OF_USE_SYSTEMUI_RESET=1 \
OF_SUPPORT_VBMETA_AVB2_PATCHING=1 \
OF_PATCH_AVB20=1 \
OF_USE_MAGISKBOOT_FOR_ALL_PATCHES=1 \
FOX_AB_DEVICE=0 \
OF_NO_TREBLE_COMPATIBILITY_LIB=0 \
OF_TREBLE_ENABLED=1 \
OF_SUPPORTS_DYNAMIC_PARTITIONS=1 \
OF_USE_GREEN_LED=0 \
FOX_RECOVERY_INSTALL_PARTITION="/dev/block/bootdevice/by-name/recovery" \
FOX_USE_BASH_SHELL=1 \
FOX_ASH_IS_BASH=1 \
OF_SUPPORT_MIUI_OTA=0 \
OF_DISABLE_MIUI_SPECIFIC_FEATURES=1 \
OF_DONT_PATCH_ON_FRESH_INSTALLATION=0 \
OF_ALLOW_DISABLE_NAVBAR=0 \
OF_SCREENSHOT=1 \
OF_FORCE_RO2RW_SYSTEM_ERASE=1 \
OF_USE_MAGISKBOOT=1 \
FOX_USE_NANO_EDITOR=1 \
FOX_USE_TWRP_RECOVERY_IMAGE_BUILDER=1 \
FOX_DELETE_ARGS_INFO_ON_INSTALL=1 \
FOX_USE_USERDATA_ON_FIRST_BOOT=1 \
FOX_USE_LZ4_BINARY=1 \
FOX_USE_ZSTD_BINARY=1 \
FOX_USE_XZ_UTILS=1 \
FOX_BUILD_BASH=0 \
FOX_ENABLE_DRAGONTC=0 \
FOX_PARTITION_INFO=1 \
FOX_DISABLE_APP_MANAGER=1
      
       source build/envsetup.sh
        set -e
        lunch twrp_${{ inputs.DEVICE_NAME }}-eng && make clean && mka adbd ${{ inputs.BUILD_TARGET }}image

    - name: Set Release Properties
      run: |
        echo "BUILD_DATE=$(TZ=Asia/Manila date +%Y%m%d)" >> $GITHUB_ENV
        cd ${GITHUB_WORKSPACE}/OrangeFox/fox_${{ inputs.MANIFEST_BRANCH }}

        DEVICE_TREE_URL="${{ inputs.DEVICE_TREE }}"
        if [[ "${DEVICE_TREE_URL}" == *.git ]]; then
          DEVICE_TREE_URL="${DEVICE_TREE_URL%.git}"
        fi
        echo "DEVICE_TREE_URL=${DEVICE_TREE_URL}" >> $GITHUB_ENV

    - name: Upload to Release
      uses: softprops/action-gh-release@v2
      with:
        files: |
          OrangeFox/fox_${{ inputs.MANIFEST_BRANCH }}/out/target/product/${{ inputs.DEVICE_NAME }}/OrangeFox*.img
          OrangeFox/fox_${{ inputs.MANIFEST_BRANCH }}/out/target/product/${{ inputs.DEVICE_NAME }}/OrangeFox*.zip
        name: OrangeFox Recovery for ${{ inputs.DEVICE_NAME }} // ${{ env.BUILD_DATE }}
        tag_name: ${{ github.run_id }}
        body: |
          ## OrangeFox Recovery Build - Unofficial
          Build: fox_${{ inputs.MANIFEST_BRANCH }}
          Device: [Device Tree/Branch](${{ env.DEVICE_TREE_URL }}/tree/${{ inputs.DEVICE_TREE_BRANCH }})
          Commit: Most recent [commit](${{ env.DEVICE_TREE_URL }}/commit/${{ env.COMMIT_ID }}) during building.
          
